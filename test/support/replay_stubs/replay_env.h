// Replay stub: minimal Arduino shims for host-native replay tests.
// These are identical to the test/mocks/ versions but kept separate
// so that the replay env can evolve independently if needed.
//
// For now this file simply re-exports the existing mock Arduino.h.
#include "../../mocks/Arduino.h"
